dependencies:
  tools:
    - type: "mcp"
      value: "figma"
      description: "Official Figma MCP server for structured reads, screenshots, and editable canvas writes"
      transport: "streamable_http"
      url: "https://mcp.figma.com/mcp"
